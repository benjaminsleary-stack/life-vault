# Lessons & preferences

_Routing/preference lessons the system has learned. Ported from life-os `lessons`._
_NOTE: verify this is non-empty after migration — a live query of the brain once
returned no lessons, so don't assume the export captured them (spec §10.1)._

<!-- migration fills this from GET /api/lessons -->
